name: Automated Tests
on: [push]
jobs:
  Testing:
    runs-on: ubuntu-latest
    steps:
      - run: npm install cypress
      - run: "./node_modules/.bin/cypress run --record --key b987a334-bb6e-427f-a568-82a125a65989 --parallel"